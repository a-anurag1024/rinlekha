# ─────────────────────────────────────────────────────────────────────────────
# RinLekha — Python dependencies
# Install order matters for GPU packages. Follow the notes below.
#
# BEFORE pip install:
#   1. Verify CUDA is available: python -c "import torch; print(torch.cuda.is_available())"
#      If torch is not installed: conda install pytorch torchvision torchaudio
#                                  pytorch-cuda=12.1 -c pytorch -c nvidia
#   2. unsloth has a non-standard install — see note at bottom of this file.
#   3. vllm install: pip install vllm  (requires CUDA 12.x, Linux/WSL2 only)
# ─────────────────────────────────────────────────────────────────────────────

# ── Phase 1: Data Pipeline ────────────────────────────────────────────────────
ray[default]>=2.10.0
openai>=1.17.0
datasets>=2.18.0
huggingface_hub>=0.22.0

# ── Phase 2: Training ─────────────────────────────────────────────────────────
# torch — managed by conda (see note above), not listed here to avoid conflicts
transformers>=4.49.0
trl>=0.8.6
peft>=0.10.0
bitsandbytes>=0.43.0
accelerate>=0.28.0
mlflow>=2.12.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# ── Phase 3/4: Evaluation ─────────────────────────────────────────────────────
deepeval>=1.4.0
openai>=1.17.0                 # Phase 1 synthesis + DeepEval LLM-as-judge + vLLM OpenAI-compat client

# ── Phase 5: Serving ──────────────────────────────────────────────────────────
# vllm — install separately: pip install vllm
langchain>=0.3.0
langchain-openai>=0.3.0
langchain-community>=0.3.0
langfuse>=2.36.0
pydantic>=2.7.0

# ── Phase 6: Demo App ─────────────────────────────────────────────────────────
gradio>=4.26.0

# ── Utilities (all phases) ────────────────────────────────────────────────────
python-dotenv>=1.0.0
pyyaml>=6.0.1
tqdm>=4.66.0
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0

# ─────────────────────────────────────────────────────────────────────────────
# UNSLOTH — install separately AFTER torch is confirmed working:
#
#   pip install "unsloth[huggingface] @ git+https://github.com/unslothai/unsloth.git"
#
# Or for the stable PyPI release:
#   pip install unsloth
#
# Unsloth requires torch with CUDA already installed. Installing it via
# requirements.txt without torch pre-installed will silently fall back to
# the CPU-only build and training will fail.
# ─────────────────────────────────────────────────────────────────────────────
