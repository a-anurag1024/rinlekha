# Template — do NOT commit real values.
# Populate and apply manually:
#   kubectl create secret generic rinlekha-secrets \
#     --namespace rinlekha \
#     --from-literal=openai-api-key=<your-key> \
#     --from-literal=hf-token=<your-token>
apiVersion: v1
kind: Secret
metadata:
  name: rinlekha-secrets
  namespace: rinlekha
type: Opaque
stringData:
  openai-api-key: "REPLACE_ME"
  hf-token: "REPLACE_ME"
