apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: eval-results-pvc
  namespace: rinlekha
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: standard
  resources:
    requests:
      storage: 1Gi
